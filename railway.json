{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "npx expo start --web --port $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
